name: Keep Hugging Face Space Awake

on:
  schedule:
    - cron: "0 0,12 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping dello Space
        run: |
          curl -s -o /dev/null -w "HTTP Status: %{http_code}\n" https://cosmoedulab-cosmoedu.hf.space
